import React from "react";
import LeftVersion from "./left-version";
import TopVersion from "./top-version";

const Catalog = () => {
  return (
    <>
      <LeftVersion />
      <TopVersion />
    </>
  );
};

export default Catalog;
